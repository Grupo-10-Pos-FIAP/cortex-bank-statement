#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Otimizado: Usa lint-staged para processar apenas arquivos modificados
# Isso é muito mais rápido que processar todo o projeto
echo "🔍 Executando lint-staged (apenas arquivos modificados)..."
npx lint-staged || {
  echo "❌ Erros encontrados. Corrija antes de commitar."
  exit 1
}

echo "✅ Pre-commit checks passaram!"
