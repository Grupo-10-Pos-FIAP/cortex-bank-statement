export const YIELD_PERCENTAGE = 3;

export const AUTH_REDIRECT_PATH = "/auth";

export const DEFAULT_PAGE_SIZE = 25;

export const DEBOUNCE_DELAY_SEARCH = 300;

export const DEBOUNCE_DELAY_FILTERS = 500;
